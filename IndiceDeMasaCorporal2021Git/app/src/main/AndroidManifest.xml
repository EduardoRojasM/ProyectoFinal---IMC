<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.indicedemasacorporal2021">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".IMCTabla"
            android:exported="true" />
        <activity android:name=".InformacionDeLaAplicacion" />
        <activity android:name=".Registro" />
        <activity android:name=".PromedioPersonas" />
        <activity android:name=".FormulaIMC" />
        <activity android:name=".IMC" />
        <activity android:name=".Introducción" />
        <activity android:name=".PantallaDeCarga">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>